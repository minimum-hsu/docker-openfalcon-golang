# docker-openfalcon-golang

## Build

Enter the following command in the repo directory.

```
$sudo docker build --force-rm=true -t openfalcon-golang .
```

## Run

```
$sudo docker run -ti --name golang openfalcon-golang
```
